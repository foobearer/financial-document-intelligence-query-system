# ── Core LLM & RAG ────────────────────────────────────────────────────────────
langchain==0.2.5               # RAG orchestration — chains, retrievers, memory
langchain-openai==0.1.8        # LangChain OpenAI integration
langchain-community==0.2.5     # Community integrations (ChromaDB, etc.)
openai==1.30.1                 # OpenAI SDK — GPT-4o for extraction & Q&A
chromadb==0.5.0                # Local vector store — no server needed
tiktoken==0.7.0                # Token counting for OpenAI models

# ── Document Processing ────────────────────────────────────────────────────────
pypdf2==3.0.1                  # PDF text extraction
pdfplumber==0.11.0             # Advanced PDF — better table extraction than PyPDF2
python-docx==1.1.2             # Word document support

# ── Data Processing ────────────────────────────────────────────────────────────
pandas==2.2.2                  # Structured data manipulation
numpy==1.26.4                  # Numerical operations

# ── API ───────────────────────────────────────────────────────────────────────
fastapi==0.111.0               # Production REST API
uvicorn[standard]==0.29.0      # ASGI server
python-multipart==0.0.9        # File upload support
pydantic==2.7.1                # Request/response validation
pydantic-settings==2.2.1       # Settings from .env

# ── Storage ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30             # ORM for document metadata
aiofiles==23.2.1               # Async file I/O

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # Load .env files
structlog==24.1.0              # Structured logging
tenacity==8.3.0                # Retry logic for OpenAI rate limits
httpx==0.27.0                  # Async HTTP client

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
