version: "3.9"
services:
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - api

  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file: .env
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000
